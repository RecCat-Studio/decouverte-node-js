let config = {};

config.node_env = process.env.NODE_ENV;
config.port = process.env.PORT;
config.format_log = process.env.FORMAT_LOG;
config.node_env = process.env.NODE_ENV;
config.version = process.env.VERSION;

export default config;